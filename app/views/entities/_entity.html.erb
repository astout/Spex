<li>
  <%= link_to entity.name, entity %>
  <% if current_user.admin? %>
    | <%= link_to "delete", entity, method: :delete,
                                  data: { confirm: "You sure?" } %>
  <% end %>
</li>